# DO NOT commit a populated version of this file.
# Create the secret with:
#   kubectl create secret generic data-lineage-secrets \
#     --namespace=data-lineage \
#     --from-literal=OPENAI_API_KEY='sk-...' \
#     --dry-run=client -o yaml | kubectl apply -f -
#
# This template is a reference only — values are intentionally blank.
apiVersion: v1
kind: Secret
metadata:
  name: data-lineage-secrets
  namespace: data-lineage
type: Opaque
stringData:
  OPENAI_API_KEY: ""
