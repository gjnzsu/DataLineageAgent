apiVersion: v1
kind: Service
metadata:
  name: data-lineage-agent
  namespace: data-lineage
  labels:
    app: data-lineage-agent
spec:
  type: LoadBalancer
  selector:
    app: data-lineage-agent
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
